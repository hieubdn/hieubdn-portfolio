# Copy to .env.local (never commit .env.local).
# Secret: pnpm dlx auth secret   OR   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=
# Optional in dev; set in production (e.g. https://your-domain.com)
AUTH_URL=
ADMIN_USERNAME=
ADMIN_PASSWORD=

NEXT_PUBLIC_SITE_URL=